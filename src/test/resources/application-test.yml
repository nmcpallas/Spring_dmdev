  datasource:
    username: postgres
    password: postgres
    url: jdbc:h2:mem:cpallas
    driver-class-name: org.h2.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        show_sql: true
        format_sql: true
        hbm2ddl:
          auto: create
